This module creates the MediaTek internal display config tool bin: PanelMaster.

This library is pre-built by MediaTek internally.


WHAT IT DOES?
=============
fbconfig is the native display pamrameters adjusted tool which is used with the kenerl level fbconfig part.

HOW IT WAS BUILT?
==================
It needs no extra static or dynamic libraries.

HOW TO USE IT?
==============
It can be used in commmand mode (Windows OS) or shell (Lniux OS) dependently.
The UI party of PanelMaster call it to read/write the display parameters.


