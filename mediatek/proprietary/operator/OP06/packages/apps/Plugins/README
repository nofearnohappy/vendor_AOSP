OP06 customization features.

This apk is pre-built by MediaTek internal.


WHAT IT DOES?
=============
It is a plugin apk which contains the OP06 customization for Host App (browser,mms and son on)

HOW IT WAS BUILT?
==================
It needs the following library from Mediatek:
1. mediatek-common.jar
2. Host App api interface,for example:com.mediatek.mms.ext

All the OP06 customization for browser,mms and son on will be built as OP06Plugin.apk  installed under system/plugin, 
and the OP06Plugin.mpinfo also be copied to the installed path when build time

HOW TO USE IT?
==============

Implement the interface provided by Host App(browser,mms and so on) to do customization.
all api interface are at (packages/apps/xxx(Host App name)/ext).

when run time, framework will find the OP06Plugin customization implementation via OP06Plugin.mpinfo.

All the source code of  were written by MediaTek.
