OP03 customization features.

This apk is pre-built by MediaTek internal.


WHAT IT DOES?
=============
It is a plugin apk which contains the OP03 customization for Host App (phone,dialer and son on)

HOW IT WAS BUILT?
==================
It needs the following library from Mediatek:
1. mediatek-common.jar
2. Host App api interface,for example:com.mediatek.dialer.ext

All the OP03 customization for phone,dialer and son on will be built as OP03Plugin.apk  installed under system/plugin, 
and the OP03Plugin.mpinfo also be copied to the installed path when build time

HOW TO USE IT?
==============

Implement the interface provided by Host App(phone,dialer and so on) to do customization.
all api interface are at (packages/apps/xxx(Host App name)/ext).

when run time, framework will find the OP03Plugin customization implementation via OP03Plugin.mpinfo.

All the source code of  were written by MediaTek.
