This module is a library that used by factory.bin and pre-built by MediaTek internally.

WHAT IT DOES?
=============
It is used to put chars or pictures on screen by factory mode.  

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:

libpng, zlib

HOW TO USE IT?
==============

It used as an static library by factory mode.
