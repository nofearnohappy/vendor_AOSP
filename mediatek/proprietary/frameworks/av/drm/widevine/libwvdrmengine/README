library of Drm features, including classic drm, modular drm, TRUSTONIC TEE or IN HOUSE TEE.

This library is pre-built by MediaTek internal.

WHAT IT DOES?
=============
liboemcrypto.so will be used by drm, it contains modular drm function.
Without them, classic drm, modular drm feature will not work.

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1. libcrypto.so
2. libcutils.so
3. libdl.so
4. liblog.so
5. libstlport.so
6. libutils.so
7. libz.so
8. libMcClient.so
9. libsec_mem.so

and the following libs from MediaTek:
1. lib_uree_mtk_modular_drm.so
2. libtlcModularDRMWidevine

All source/dependency modules of this module are already put in
'system/lib' folder.

HOW TO USE IT?
==============
liboemcrypto.so provides function of classic drm and modular drm.
