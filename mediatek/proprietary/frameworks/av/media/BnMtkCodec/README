Library of MTK ape decode features, including Bn codec service.

This library is pre-built by MediaTek internal.


WHAT IT DOES?
=============
libBnMtkCodec.so contains MediaTek developed ape decoder related  modules.  
Without it, ape will not decode.

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1. libbinder.so
2. libutils.so
3. libcutils            
4. libdl.so                   
5. libui.so

and the following libs from MediaTek:
1.  libapedec_mtk.so

All source/dependency modules of this module are already put in
'vendor/mediatek/proprietary/frameworks/av/media/BnMtkCodec' folder.

HOW TO USE IT?
==============

Provide codec interface for ape componenet to transfer bitstream to swip 


All the source code of this library were written by MediaTek co..
