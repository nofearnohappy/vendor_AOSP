Library of MTK telephony extension interfaces. These interfaces extends the
original interface with MSIM functionality.

This library is pre-built by MediaTek internal.


WHAT IT DOES?
=============
libsimmelock shared library contains Mediatek developed OEM layer for T-Mobile 
remote sim lock feature. Trustonic module can use these interfaces to communicate 
with modem side. 

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1. N/A

and the following libs from MediaTek:
1. N/A

HOW TO USE IT?
==============
We provide fives interfaces as following:
1. get shared key
2. update sim lock blob
3. get sim lock blob version
4. reset sim lock blob
5. get modem status 
Applications can call related interface to finish their requirement.

All the source code of this library were written by MediaTek co..
