This module is used as a static library for Auto testing.

This library is pre-built jars.


WHAT IT DOES?
=============
It only contains pre-built jar libraries needed for Auto testing.

HOW IT WAS BUILT?
==================
Build directly.

HOW TO USE IT?
==============
In other Testing projects, if it needs some libraries provided here.
just declair in it's makefiles.
LIKE: LOCAL_STATIC_JAVA_LIBRARIES := librobotium4
