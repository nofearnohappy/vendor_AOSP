JNI Library of engineermode gpio, which contain the interfaces
to communicate with gpio driver

This library is pre-built by MediaTek internal.

WHAT IT DOES?
=============
libem_gpio_jni.so provide jni interface to EngineerMode application to 
access native interface of gpio

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1. libutils
2. libcutils


and the following libs from MediaTek:
1. libem_support_jni

All source/dependency modules of this module are already put in
'vendor/mediatek/libs' folder.

HOW TO USE IT?
==============

provide jni interface to EngineerMode to access native interface of gpio
all api interface are at (vendor/mediatek/proprietary/packages/apps/EngineerMode/jni/gpio)

All the source code of this library were written by MediaTek co..
