The bin file 'lcdc_screen_cap' is a debug tool for catching display raw picture.
This bin is pre-built by MediaTek internal.


WHAT IT DOES?
=============

capture display raw picture from kernel

HOW IT WAS BUILT?
==================

All source/dependency modules of this module are already put in
'vendor/mediatek/libs' folder.


HOW TO USE IT?
==============

The bin file is a debug tool, and can get display raw picture from kernel.
