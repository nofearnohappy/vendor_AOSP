This module is to dump mlog info.


WHAT IT DOES?
=============
mlog records memory info in kernel buffer.
This tool reads these info and dumps it out.

HOW IT WAS BUILT?
==================
It doesn't need other lib.

HOW TO USE IT?
==============

Invoke this bin with following command:
adb shell dmlog

The majority of source code for this library were written by MediaTek.  Various
open source libraries are also used and contained in this folder.
